import { Component } from '@angular/core';
import { MatModule } from '../../../MatModule';

@Component({
  selector: 'app-stats',
  imports: [MatModule],
  templateUrl: './stats.html',
  styleUrl: './stats.css',
})
export class Stats {}
